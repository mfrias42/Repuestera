// Test ultra-simplificado que solo verifica que Jest puede ejecutar tests
// Sin imports, sin mocks, sin dependencias externas
test('Jest está funcionando correctamente', () => {
  expect(true).toBe(true);
});
