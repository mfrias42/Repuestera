# Configuración del servidor
PORT=8000
NODE_ENV=development

# Configuración de la base de datos SQLite
# SQLite no requiere configuración de host, puerto, usuario o contraseña
# El archivo de base de datos se crea automáticamente en backend/data/repuestera.db

# JWT Secret
JWT_SECRET=tu_jwt_secret_muy_seguro_aqui
JWT_EXPIRES_IN=24h

# Configuración de archivos
UPLOAD_PATH=uploads/
MAX_FILE_SIZE=5242880